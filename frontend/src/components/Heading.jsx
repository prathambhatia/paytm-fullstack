export function Heading({ title }) {
  return <h2 className="text-xl font-bold font-sans mb-4">{title}</h2>;
}

export function SubHeading({title}) {
    return <h3 className = "text-[#E0E0E0]">{title}</h3>
}
